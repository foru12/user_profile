import 'package:flutter/material.dart';
class avatar extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container( //еще один контейнер с заданными высотой и шириной
      width: 100,
      height: 100,
      child: Placeholder(),
    );
  }
}
